coordinator.example.internal {
  encode gzip

  # Replace with your internal CA or managed certificate path.
  tls /etc/ssl/private/coordinator.crt /etc/ssl/private/coordinator.key

  @api {
    path /jobs* /admin/cleanup
  }

  handle @api {
    reverse_proxy 127.0.0.1:8080
  }
}
